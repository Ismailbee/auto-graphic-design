// Lightweight compatibility wrapper.
// Many components expect `useCanvasStore` to be available under this filename.
// Re-export the main canvas store implemented in `canvas.js`.
export { useCanvasStore } from './canvas';
